CREATE TABLE IF NOT EXISTS PRODUCTO (
                                      id INT AUTO_INCREMENT PRIMARY KEY,
                                      nombre VARCHAR(25),
                                      precio INT
);
